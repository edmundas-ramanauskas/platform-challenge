version: '3.4'

services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    command: npm start
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
    volumes:
      - ./:/home/app
      - node_modules_webapp:/home/app/node_modules

volumes:
  node_modules_webapp:
